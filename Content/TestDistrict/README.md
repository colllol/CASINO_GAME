# Test District

Offline Phase 1 content boundary. `ATestDistrictActor` creates a small walkable floor,
one job marker (`JOB_TEST_DELIVERY`), and one `ACasinoTableInteraction` actor using Engine
basic-shape assets. This text asset intentionally keeps the repository editor-free until
Unreal is available to create authored `.umap` and Blueprint assets.
